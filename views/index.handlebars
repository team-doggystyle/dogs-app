<div class="short-header-page wsite-theme-light wsite-page-index full-width-on touch">
  <div id="header-wrap">
    <div id="logo">
      <span class="wsite-logo">
        <a href="/">
          <img alt="Cause For SB Paws" src="/assets/img/logo1.jpg">
          </img>
        </a>
      </span>
    </div>
  </div>
  {{> navbar }}
  <div id="banner-wrap">
    <div class="home_background">
    </div>
  </div>
  <div id="main-wrap">
    <h2 class="wsite-content-title" style="margin-top: 5px;">
      JOIN THE CAUSE FOR SB PAWS!
    </h2>
    <h2>
      Recently adopted dogs:
    </h2>

    <div class="row dog_container">
      <div class="col-xs-12 media">
        {{#each dogs}} {{#if adopted}}
        <div class="col-xs-12 col-sm-4 dog_image">
          <img src="{{this.photo}}" class="dog_image">
          <div class="dog_overlay">
            <div class="dog_name">{{this.name}}</div>
            <div class="dog_breed">{{this.breed}}</div>
          </div>
        </div>

        {{/if}} {{/each}}
      </div>
    </div>

  </div>
</div>
</div>